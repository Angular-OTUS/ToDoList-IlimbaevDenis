import { EnterControl } from './enter-control';

describe('EnterControl', () => {
  it('should create an instance', () => {
    const directive = new EnterControl();
    expect(directive).toBeTruthy();
  });
});
